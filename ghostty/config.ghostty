# Non-"terminal-ish" setup: a humanist monospace with real kerning/spacing,
# plus a soft, muted theme (auto-switches with system light/dark mode).

font-family = Cascadia Code
font-size = 14
adjust-cell-width = 8%
adjust-cell-height = 15%

theme = Dayfox

cursor-style = bar
